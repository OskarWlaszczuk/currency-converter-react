import "./style.css";

const Resualts = ({ resualt }) => {
    return (
        <p className="resualts">
            {
                resualt
            }
        </p>
    );
};

export default Resualts;