import "./style.css";

const Results = ({ result }) => {
    return (
        <p className="resualts">
            {
                result
            }
        </p>
    );
};

export default Results;